services:
  frontend:
    build:
      context: ./frontend
      dockerfile: Dockerfile
    ports: ['8080:80']
    depends_on: [backend]
    restart: unless-stopped

  backend:
    build:
      context: ./backend
      dockerfile: Dockerfile
    env_file: ./backend/.env
    ports: ['4000:4000']
    restart: unless-stopped
